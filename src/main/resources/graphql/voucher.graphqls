type Voucher {
    id: String
    percentage: Float
    expire: String
}

extend type Query {
    voucher(id: String): Voucher
    vouchers: [Voucher]
}

type Mutation {
    addVoucher(id: String, percentage: Float, expire: String): Voucher
    deleteVoucher(id: String): Boolean
}